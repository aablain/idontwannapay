export class Project {
  constructor (public name: string, public details: string, public manager: string, public goal: number, public moneyraised: number, public reward: string, public type: string) { }

}
